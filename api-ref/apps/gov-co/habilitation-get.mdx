---
title: "Fetch habilitación status"
openapi: "GET /apps/gov-co/v1/habilitation/{silo_entry_id}"
---

Returns the supplier's current DIAN habilitación state: `status` is omitted until a test set has been submitted, then moves through `pending` to `accepted` or `failed`.

<Note>
  Use this endpoint to drive your own onboarding UI instead of the hosted portal. The silo entry must belong to the authenticated workspace and have been registered with the Colombia app first.
</Note>
