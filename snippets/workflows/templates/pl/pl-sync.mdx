---
title: "Sync Received invoices from KSeF"
description: "Create import jobs for each received invoice of a party in KSeF"
countryCode: "pl"
appId: "gov-pl"
schema: "org/party"
---

```json Example Sync Received invoices from KSeF workflow with states
{
    "name": "Sync Received invoices from KSeF",
    "description": "Create import jobs for each received invoice of a party in KSeF",
    "schema": "org/party",
    "steps": [
        {
            "id": "5bddfd40-0220-11f1-bd80-c7df39ccdf0b",
            "name": "Sync received invoices from KSeF",
            "provider": "gov-pl.sync"
        }
    ],
    "rescue": []
}
```