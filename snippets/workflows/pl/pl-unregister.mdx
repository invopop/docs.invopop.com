---
title: "KSeF unregister supplier (send)"
description: "Unregister a supplier from KSeF (send)"
countryCode: "pl"
appId: "gov-pl"
schema: "org/party"
---

```json Example KSeF unregister supplier (send) workflow
{
    "name": "KSeF unregister supplier (send)",
    "description": "Unregister a supplier from KSeF (send)",
    "schema": "org/party",
    "steps": [
        {
            "id": "9e9514b0-fb6e-11f0-8a4e-79beeb9e3cf2",
            "name": "Set state",
            "provider": "silo.state",
            "summary": "Set state to `processing`{.state .processing}",
            "config": {
                "state": "processing"
            }
        },
        {
            "id": "a5ec8180-fb6e-11f0-8a4e-79beeb9e3cf2",
            "name": "Unregister supplier from KSeF",
            "provider": "gov-pl.unregister"
        },
        {
            "id": "a8c06200-fb6e-11f0-8a4e-79beeb9e3cf2",
            "name": "Set state",
            "provider": "silo.state",
            "summary": "Set state to `void`{.state .void}",
            "config": {
                "state": "void"
            }
        }
    ],
    "rescue": [
        {
            "id": "aebf6ac0-fb6e-11f0-8a4e-79beeb9e3cf2",
            "name": "Set state",
            "provider": "silo.state",
            "summary": "Set state to `error`{.state .error}",
            "config": {
                "state": "error"
            }
        }
    ]
}
```