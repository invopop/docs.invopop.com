<AccordionGroup>
  <Accordion title="¿ What format does KSeF expect for periodic reports?">
    No periodic format — only the per-invoice FA(3) XML submitted via the KSeF API. JPK_V7 is a separate Ministry of Finance schema submitted via e-Deklaracje.
  </Accordion>
</AccordionGroup>
