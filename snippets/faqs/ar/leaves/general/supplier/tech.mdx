  <Accordion title="¿ How are supplier credentials stored in Invopop for Argentina?">
    The CSD certificate (`.crt`) and private key (`.key`) are stored encrypted at rest. Invopop only uses them to authenticate WSFE requests on the supplier's behalf and never returns the key in API responses.
  </Accordion>
