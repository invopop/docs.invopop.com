<AccordionGroup>
  <Accordion title="¿ What format does NF-e expect for periodic reports?">
    NF-e itself uses real-time XML submission. Aggregate periodic returns use the SPED layouts (pipe-delimited TXT signed with the A1 certificate). Invopop does not generate SPED files — only per-document NF-e XMLs.
  </Accordion>
</AccordionGroup>
