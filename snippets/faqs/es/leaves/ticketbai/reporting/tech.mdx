<AccordionGroup>
  <Accordion title="¿ What format does TicketBAI expect for periodic reports?">
    Per-invoice TicketBAI XML (the `T_TicketBai` schema, with foral variants). Bizkaia's LROE accepts an XML batch upload via a separate web service. There is no aggregate format outside Bizkaia.
  </Accordion>
</AccordionGroup>
