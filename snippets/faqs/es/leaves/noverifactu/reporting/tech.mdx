<AccordionGroup>
  <Accordion title="¿ What format does No-VERI*FACTU expect for periodic reports?">
    No periodic format. On-demand declarations use the same `RegistroAlta`/`RegistroAnulacion` schema as VERI\*FACTU plus a separate `Evento` schema for system events. AEAT can also request the full chain in raw form.
  </Accordion>
</AccordionGroup>
