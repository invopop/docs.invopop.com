<AccordionGroup>
  <Accordion title="¿ What format does VERI*FACTU expect for periodic reports?">
    No periodic format — only the per-invoice `RegistroAlta` and `RegistroAnulacion` SOAP envelopes defined in AEAT's VERI\*FACTU schema (current version 1.0).
  </Accordion>
</AccordionGroup>
